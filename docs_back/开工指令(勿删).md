git 仓库 https://github.com/dengjiacheng/Nova/tree/main/docs
本地地址 /Users/dengjiacheng/djc_nova/docs

请阅读 docs/operations/onboarding.md，按指南执行以下步骤：
完整阅读通用规范与流程，并根据自身负责的组件（NovaServer / NovaDesk / NovaAgent）继续阅读对应章节。
按文档要求制定或更新本次迭代的 To-Do，所有任务需关联相关文档段落。
开发前若发现文档缺失或需要调整，先补充/修改 docs/ 中的内容，再开始编码。
开发过程遵循 docs/operations/coding_guidelines.md 的分层、模块化、测试、日志等规范。
开发完成后更新 To-Do 状态、注明提交记录，并告知受影响团队（如有接口变更）。
【本会话负责组件】：NovaServer / NovaDesk / NovaAgent 整体联调
请从上述步骤开始执行并保持文档同步。

export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export PATH="$HOME/djc_nova/tools/flutter/bin:$PATH"
flutter run -d macos --debug

export REMOTE_HOST="root@120.26.30.221"
export SSH_PORT=22
export SSH_PASSWORD="Djc258456" # 建议改成 SSH key
export REMOTE_DIR="/opt/nova"
export PYTHON_BIN="python3.11"
./scripts/deploy_server.sh

export http_proxy="http://127.0.0.1:7890"
export https_proxy="http://127.0.0.1:7890"
