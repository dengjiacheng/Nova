# NovaAgent Iteration To-Do

| Status | Task | Doc Reference | Notes |
| --- | --- | --- | --- |
| DONE | Harden reconnect handshake to resend `REGISTER` with `recentExecutions` plus heartbeat metrics when WS reconnects. | docs/agent/lifecycle.md Section 4 心跳与健康检查 | Implemented via `NovaAgent/src/main/kotlin/com/nova/agent/core/ws/AgentWebSocketClient.kt` with payload builder/tests; awaiting final commit. |
| DONE | Implement `CANCEL` propagation that transitions `RUNNING -> STOPPING -> IDLE` and returns `FAILED` with `CANCELLED_BY_SERVER`. | docs/agent/command_handling.md Section CANCEL | Delivered via `NovaAgent/src/main/kotlin/com/nova/agent/execution/ExecutionCoordinator.kt` with coverage in `NovaAgent/src/test/kotlin/com/nova/agent/execution/ExecutionCoordinatorTest.kt`; repeated cancel now yields `NO_TASK_RUNNING`. |
| DONE | Expand script catalog metadata to include capability flags and checksum in `REGISTER` payload. | docs/agent/script_catalog.md Section 脚本版本管理 | Implemented via `NovaAgent/src/main/kotlin/com/nova/agent/execution/catalog/ScriptCatalogProvider.kt` and covered by `.../ScriptCatalogProviderTest.kt`; descriptors now emit checksum + capabilities. |
| DONE | Build Reporter offline queue with attachment handling (Base64 inline vs uploaded URL) and replay on reconnect. | docs/agent/reporting.md Section 回传策略 | Implemented via `NovaAgent/src/main/kotlin/com/nova/agent/reporting/ReliableReporter.kt` 与 `PersistentReporterQueue.kt`，测试见 `NovaAgent/src/test/kotlin/com/nova/agent/reporting/*Reporter*Test.kt`；现已扩展为持久化队列并支持 ACK 重放。 |
| DONE | Verify execution command parsing matches envelope fields (`assets`, `options`) before Runner instantiation. | docs/architecture/message_protocol.md Section 执行指令示例 | Implemented via `NovaAgent/src/main/kotlin/com/nova/agent/execution/command/ExecuteCommandParser.kt` with tests in `NovaAgent/src/test/kotlin/com/nova/agent/execution/command/ExecuteCommandParserTest.kt`; logging now captures normalized command context. |
| TODO | 联调准备：落地 Agent 启动参数与重连流程验收脚本 | docs/agent/lifecycle.md Section 1 启动, docs/flows/core_flows.md Section 2 设备发现与上线 | 设计 instrumentation 脚本驱动 `adb install/start`、记录 REGISTER/ACK/HEARTBEAT 序列，输出供 NovaDesk/NovaServer 联调复现的步骤与环境变量模板。 |
| TODO | 联调阶段：执行回报链路对齐与日志级别策略 | docs/agent/reporting.md Section 回传策略, docs/architecture/message_protocol.md Section WebSocket 消息 Envelopes | 明确执行进度/日志/附件在联调期的采样策略，提供可配置的 Reporter 调试开关与故障回放 checklist，确保跨端 traceId 可对齐。 |
| TODO | 适配 Admin Tools 上线的 Agent 包版本管理 | docs/agent/script_catalog.md Section 脚本更新流程, docs/server/rest_api.md Section 7 Agent 包管理 | 在 Agent 启动/重连握手中补充 `versionCode` 校验逻辑，支持 Server 强制升级策略，并更新测试覆盖新版本通告场景。 |
| DONE | 架构审查：梳理 NovaAgent 模块职责与膨胀风险，输出重构建议草案 | docs/agent/overview.md Section 模块划分, docs/agent/command_handling.md Section 状态机定义, docs/agent/lifecycle.md Section 3 执行任务 | `2025-10-21` 本会话完成：引入 `AgentKernel`/`AgentMessageRouter`/`WebSocketReporter`（见 `NovaAgent/core/src/main/kotlin/com/nova/agent/core/...`），文档同步更新；后续跟进拆解为 T-A1/T-A2。 |
| IN_PROGRESS | 三端联调：Agent↔Server 消息契约勘误与回放脚本梳理 | docs/architecture/message_protocol.md Section WebSocket 消息 Envelopes, docs/agent/reporting.md Section 回传策略, docs/server/websocket.md Section Agent 通道 | 本会话整理 REGISTER/EXECUTE/REPORT 实际 payload 与文档差异，记录需修正字段、错误码及回放脚本计划，支撑后续联调冒烟测试。 |
| TODO | T-A1：对齐 Server/Desk WS Topic 与 REGISTER/REPORT Payload 契约 | docs/agent/lifecycle.md Section 1 启动, docs/architecture/message_protocol.md Section WebSocket 消息 Envelopes, docs/server/websocket.md Section Agent 通道 | 校验 `REGISTER`/`HEARTBEAT`/`EXECUTE` 回报字段与最新契约一致，补充契约测试（含 topic/traceId），避免桌面端旧枚举解包失败。 |
| TODO | T-A2：输出联调期 Agent 诊断脚本与日志对齐清单 | docs/agent/reporting.md Section 回传策略, docs/flows/core_flows.md Section 5 脚本执行, docs/operations/coding_guidelines.md Section 日志与追踪 | 产出 adb 启动脚本、WS 抓包示例与日志字段映射，便于 NovaDesk/Server 重构后快速定位联调问题。 |
| TODO | T-A3：实现持久化 Reporter 队列与身份隔离（含 ACK 机制） | docs/agent/reporting.md Section 回传策略, docs/architecture/message_protocol.md Section 进度/结果 ACK | 落地 `PersistentReporterQueue`（SQLite/文件），队列主体写入 `tenantId/userId/pcId/deviceId`，实现 ACK 删除与账号切换清理，并为断线重放补充单测。 |
| TODO | T-A4：命令 ACK 与执行/WS 调度隔离 | docs/agent/command_handling.md Section EXECUTE, docs/agent/lifecycle.md Section 3 执行任务 | Agent 接到命令后返回 `executions.ack`，拒绝时附错误码；执行部分迁移到独立单线程 dispatcher，保证长耗时脚本不影响 WS 收发。 |
